<%@docs.each do |doc|%>

    <h2> <%= doc.id %>. <%= link_to doc.title, doc %> </h2>
    
    <p> <%= time_ago_in_words(doc.created_at)%> </p>

    <p> <%= truncate(doc.content, length:100) %> </p>
    
    
    <br>

<%end%>